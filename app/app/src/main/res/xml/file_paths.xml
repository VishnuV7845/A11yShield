<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Grant access to the "reports" subdirectory within getFilesDir() -->
    <files-path
        name="internal_reports"
        path="reports/" />
    <!-- The name="internal_reports" is arbitrary, but path="reports/" MUST match -->
</paths>