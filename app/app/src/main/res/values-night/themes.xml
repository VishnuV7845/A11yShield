<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Use NoActionBar as default -->
    <style name="Theme.A11yShield" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorAccent</item> <!-- Use accent as primary -->
        <item name="colorPrimaryVariant">@color/colorAccent</item> <!-- Can be same or darker -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Accent color -->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Status bar color. -->
        <!-- Match primary or use a specific color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/white</item>

        <!-- Style Material Buttons -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <!-- Style Tabs -->
        <item name="tabStyle">@style/Widget.App.TabLayout</item>
    </style>

    <!-- Specific Theme for MainActivity if needed (can inherit) -->
    <style name="Theme.A11yShield.NoActionBar" parent="Theme.A11yShield">
        <!-- Ensures no default action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Specific style for buttons -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>

    <!-- Specific style for TabLayout -->
    <style name="Widget.App.TabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabIndicatorColor">@color/colorAccent</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="tabTextColor">@color/tab_text_color</item>
        <item name="tabBackground">@color/white</item>
    </style>

    <!-- Theme for Splash Screen (No Changes Needed) -->
    <style name="Theme.A11yShield.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <!-- Removed AppBarLayout style -->

</resources>